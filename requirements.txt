kivy>=2.3.0
numpy>=1.24.0
Pillow>=10.0.0
opencv-python>=4.9.0.0  # image processing for lesion segmentation
rembg>=2.0.50  # background removal for leaf extraction
onnxruntime>=1.16.0  # required backend for rembg and classification

# Image analysis and feature extraction
pandas>=2.0.0  # CSV output for leaf features
scikit-image>=0.21.0  # texture analysis (graycomatrix, graycoprops)

# Hardware interfaces for Raspberry Pi
RPi.GPIO>=0.7.1  # GPIO control for motor/LED

# Camera system packages (install via apt, not pip):
# sudo apt install -y libcamera-apps python3-libcamera python3-picamera2

# Optional: TFLite runtime (lighter than full tensorflow)
# tflite-runtime==2.13.0
# Hardware interfaces (typically pre-installed on Raspberry Pi OS):
# picamera2
# RPi.GPIO
# Optional smaller runtime (prefer over full tensorflow for deployment); install one of:
# tflite-runtime==2.13.0
